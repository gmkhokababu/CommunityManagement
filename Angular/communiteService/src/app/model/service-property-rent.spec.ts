import { ServicePropertyRent } from './service-property-rent';

describe('ServicePropertyRent', () => {
  it('should create an instance', () => {
    expect(new ServicePropertyRent()).toBeTruthy();
  });
});
