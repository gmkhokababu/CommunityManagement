import { AdvertisementProperty } from './advertisement-property';

describe('AdvertisementProperty', () => {
  it('should create an instance', () => {
    expect(new AdvertisementProperty()).toBeTruthy();
  });
});
